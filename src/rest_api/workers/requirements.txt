# Lambda Workers Dependencies
# Shared dependencies for both agent_worker and delivery_worker Lambda functions

# AWS SDK
boto3>=1.35.0

# Data validation and settings management
pydantic>=2.0.0
pydantic-core
pydantic-settings>=2.0.0

# AWS Lambda Powertools for logging, metrics, tracing
aws-lambda-powertools>=2.0.0

# HTTP client (for delivery_worker if needed for REST fallback)
requests>=2.32.0
